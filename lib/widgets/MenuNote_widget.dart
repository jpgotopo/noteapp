import 'package:flutter/material.dart';

Widget menuNote() {
  return Container(
      color: Colors.blue,
      child: Center(
          child:
              Text("Menu Note", style: TextStyle(color: Colors.blueAccent))));
}
