import 'package:flutter/material.dart';

Widget detailNote() {
  return Container(
      color: Colors.red,
      child: Center(
          child:
              Text("Detail Note", style: TextStyle(color: Colors.blueAccent))));
}
